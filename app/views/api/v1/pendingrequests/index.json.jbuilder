# json.array! @requests do |req|
# 	json.id req.id
# end

# json.requests @requests do |req|
#   json.(req, :id, :from_user_id, :to_user_id, :amount, :aproved, :declined, :seen, :expdate, :timestamp)
# end
